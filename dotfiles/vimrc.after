" don't need to turn off compatible mode if vim sources a user .vimrc
"set nocompatible

" set color scheme
colorscheme desert

" enable file type based indent configuration
filetype plugin indent on

" turn on search highlighting
set hlsearch

" show line number
set number

" enable syntax highlighting
syntax on
